---
id: "01M2JVC5Q98DYH3YN9HE71RBHW"
schemaVersion: "1.0"
createdBy: "01M1VDQX7KEACQQ8KY2HZ5DTFK"
createdAt: "2026-09-15T15:36:09.884Z"
type: "Statement"
supersedes: null
problemId: "01M2JMFAPYEDSSTG9R7CWHJGQ4"
version: 1
digest: "sha256:6866a65f800bb0cca65412513d396087c2977741346fbf92e98aff8618f5d1a2"
clauses: [{"id":"main","label":"Problem","text":"How many copies of an unknown low-rank $N$-qubit state does tomography with Pauli measurements need?\n\nLet $d=2^N$ and let $\\rho$ be a density operator on $(\\mathbb{C}^2)^{\\otimes N}$ with $\\operatorname{rank}(\\rho)\\leq r$, where $1\\leq r\\leq d$ is known to the algorithm. A Pauli measurement is labeled by a string $s\\in\\{X,Y,Z\\}^N$. It measures every qubit $j$ in the eigenbasis of the Pauli operator $\\sigma_{s_j}$ and records all $N$ outcomes $x\\in\\{-1,1\\}^N$, so its POVM elements are\n\\begin{equation}\n  M^{s}_{x}=\\bigotimes_{j=1}^{N}\\frac{I+x_j\\,\\sigma_{s_j}}{2},\n  \\qquad\n  \\sigma_X=X,\\quad\\sigma_Y=Y,\\quad\\sigma_Z=Z.\n  \\label{eq:df68-pauli-povm}\n\\end{equation}\nAn algorithm consumes one fresh copy of $\\rho$ per measurement, and the string $s$ used on a copy may depend on the outcomes recorded on all earlier copies. After $n$ copies it outputs a density operator $\\hat\\rho$. It succeeds on $\\rho$ if\n\\begin{equation}\n  \\tfrac12\\lVert\\hat\\rho-\\rho\\rVert_1\\leq\\epsilon\n  \\label{eq:df68-error}\n\\end{equation}\nholds with probability at least $2/3$. Let $n_{\\mathrm{P}}(N,r,\\epsilon)$ be the least $n$ for which some algorithm using only measurements of the form \\eqref{eq:df68-pauli-povm} satisfies \\eqref{eq:df68-error} on every state of rank at most $r$. Replacing $2/3$ by any other constant in $(1/2,1)$ changes $n_{\\mathrm{P}}$ by at most a constant factor.\n\nDetermine how $n_{\\mathrm{P}}(N,r,\\epsilon)$ grows with $N$ and $r$. In exponent form, fix $\\epsilon\\in(0,1/2)$ and for $0\\leq\\alpha\\leq1$ set\n\\begin{equation}\n  c(\\alpha):=\\lim_{N\\to\\infty}\\frac{1}{N}\\log_2 n_{\\mathrm{P}}\\bigl(N,\\lceil2^{\\alpha N}\\rceil,\\epsilon\\bigr),\n  \\label{eq:df68-exponent}\n\\end{equation}\nwith the limit inferior and the limit superior taken separately if the limit does not exist. The endpoints are known, $c(0)=1$ and $c(1)=\\log_2 10$. For $0<\\alpha<1$ the known bounds are\n\\begin{equation}\n  1+\\alpha\\leq c(\\alpha)\\leq\\min\\{\\log_2 3+2\\alpha,\\ \\log_2 10\\},\n  \\label{eq:df68-known-bounds}\n\\end{equation}\nand the lower bound improves to $1+2\\alpha$ for nonadaptive algorithms, those whose strings $s$ are all fixed before the first copy is measured. Determine $c(\\alpha)$ for every $\\alpha\\in(0,1)$, both for the adaptive algorithms defined above and for nonadaptive algorithms. Upper and lower bounds on $n_{\\mathrm{P}}(N,r,\\epsilon)$ that agree up to a factor polynomial in $N$ for every $r$ would settle the question.","kind":"decision","resolutionCriteria":"Resolve the authored problem statement as stated.","supersedesClauseId":null,"quantity":null}]
---
How many copies of an unknown low-rank $N$-qubit state does tomography with Pauli measurements need?

Let $d=2^N$ and let $\rho$ be a density operator on $(\mathbb{C}^2)^{\otimes N}$ with $\operatorname{rank}(\rho)\leq r$, where $1\leq r\leq d$ is known to the algorithm. A Pauli measurement is labeled by a string $s\in\{X,Y,Z\}^N$. It measures every qubit $j$ in the eigenbasis of the Pauli operator $\sigma_{s_j}$ and records all $N$ outcomes $x\in\{-1,1\}^N$, so its POVM elements are

$$
M^{s}_{x}=\bigotimes_{j=1}^{N}\frac{I+x_j\,\sigma_{s_j}}{2},
 \qquad
 \sigma_X=X,\quad\sigma_Y=Y,\quad\sigma_Z=Z.
\tag{1}
$$

An algorithm consumes one fresh copy of $\rho$ per measurement, and the string $s$ used on a copy may depend on the outcomes recorded on all earlier copies. After $n$ copies it outputs a density operator $\hat\rho$. It succeeds on $\rho$ if

$$
\tfrac12\lVert\hat\rho-\rho\rVert_1\leq\epsilon
\tag{2}
$$

holds with probability at least $2/3$. Let $n_{\mathrm{P}}(N,r,\epsilon)$ be the least $n$ for which some algorithm using only measurements of the form (1) satisfies (2) on every state of rank at most $r$. Replacing $2/3$ by any other constant in $(1/2,1)$ changes $n_{\mathrm{P}}$ by at most a constant factor.

Determine how $n_{\mathrm{P}}(N,r,\epsilon)$ grows with $N$ and $r$. In exponent form, fix $\epsilon\in(0,1/2)$ and for $0\leq\alpha\leq1$ set

$$
c(\alpha):=\lim_{N\to\infty}\frac{1}{N}\log_2 n_{\mathrm{P}}\bigl(N,\lceil2^{\alpha N}\rceil,\epsilon\bigr),
\tag{3}
$$

with the limit inferior and the limit superior taken separately if the limit does not exist. The endpoints are known, $c(0)=1$ and $c(1)=\log_2 10$. For $0<\alpha<1$ the known bounds are

$$
1+\alpha\leq c(\alpha)\leq\min\{\log_2 3+2\alpha,\ \log_2 10\},
\tag{4}
$$

and the lower bound improves to $1+2\alpha$ for nonadaptive algorithms, those whose strings $s$ are all fixed before the first copy is measured. Determine $c(\alpha)$ for every $\alpha\in(0,1)$, both for the adaptive algorithms defined above and for nonadaptive algorithms. Upper and lower bounds on $n_{\mathrm{P}}(N,r,\epsilon)$ that agree up to a factor polynomial in $N$ for every $r$ would settle the question.
