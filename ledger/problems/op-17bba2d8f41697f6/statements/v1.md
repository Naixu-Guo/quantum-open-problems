---
id: "01M21QKF09D47YN9BBSVC4EEDG"
schemaVersion: "1.0"
createdBy: "01M1VDQX7KEACQQ8KY2HZ5DTFK"
createdAt: "2026-09-09T00:03:08.666Z"
type: "Statement"
supersedes: null
problemId: "01M21QD4KR3RBCKD1WEK2XSD4K"
version: 1
digest: "sha256:abec7b3498fc3d03d1efdff95e8129123e09e178cad6be7035ba904b9450eba0"
clauses: [{"id":"main","label":"Problem","text":"Does finding an explicit ENTRANCE-to-EXIT path in the standard Welded Tree\noracle problem require exponentially many quantum queries?\n\nFor a positive integer $n$, construct a graph $G_n$ from two complete binary\ntrees of depth $n$.  Designate the root of the left tree as ENTRANCE $s$ and\nthe root of the right tree as EXIT $t$.  Join the $2^n$ leaves of the left\ntree and the $2^n$ leaves of the right tree into a uniformly random cycle\nthat alternates between leaves of the two trees.  Assign the vertices\ndistinct random binary labels of length $2n$.\n\nThe algorithm is given the label of $s$ and coherent quantum-query access to\nan adjacency-list oracle.  On a valid vertex label, the oracle returns the\nlabels of its neighboring vertices, with distinguished invalid outputs used\nwhen necessary; querying arbitrary binary strings therefore does not reveal\nglobal information about the graph unless the corresponding vertex labels\nhave first been discovered.\n\nThe path-finding task is to output an explicit sequence of vertex labels\n$v_0,v_1,\\ldots,v_\\ell$ satisfying\n\\begin{equation}\n  v_0=s,\n  \\qquad\n  v_\\ell=t,\n  \\qquad\n  \\{v_{j-1},v_j\\}\\in E(G_n)\n  \\quad\n  \\text{for every }1\\leq j\\leq\\ell .\n  \\label{eq:17bb-weld-path}\n\\end{equation}\nLet $Q_{\\mathrm{WTPath}}(n)$ denote the minimum number of oracle queries\nused by a quantum algorithm that outputs a valid path satisfying\n\\eqref{eq:17bb-weld-path} with bounded error on the standard random Welded\nTree oracle distribution.  The open question is whether there exists a\nconstant $c>0$ such that, for all sufficiently large $n$,\n\\begin{equation}\n  Q_{\\mathrm{WTPath}}(n)\n  \\geq\n  2^{cn}.\n  \\label{eq:17bb-weld-exponential}\n\\end{equation}\nEquivalently, does the exponential quantum-query lower bound in\n\\eqref{eq:17bb-weld-exponential} hold for unrestricted quantum algorithms?","kind":"decision","resolutionCriteria":"Resolve the authored problem statement as stated.","supersedesClauseId":null,"quantity":null}]
---
Does finding an explicit ENTRANCE-to-EXIT path in the standard Welded Tree oracle problem require exponentially many quantum queries?

For a positive integer $n$, construct a graph $G_n$ from two complete binary trees of depth $n$. Designate the root of the left tree as ENTRANCE $s$ and the root of the right tree as EXIT $t$. Join the $2^n$ leaves of the left tree and the $2^n$ leaves of the right tree into a uniformly random cycle that alternates between leaves of the two trees. Assign the vertices distinct random binary labels of length $2n$.

The algorithm is given the label of $s$ and coherent quantum-query access to an adjacency-list oracle. On a valid vertex label, the oracle returns the labels of its neighboring vertices, with distinguished invalid outputs used when necessary; querying arbitrary binary strings therefore does not reveal global information about the graph unless the corresponding vertex labels have first been discovered.

The path-finding task is to output an explicit sequence of vertex labels $v_0,v_1,\ldots,v_\ell$ satisfying

$$
v_0=s,
 \qquad
 v_\ell=t,
 \qquad
 \{v_{j-1},v_j\}\in E(G_n)
 \quad
 \text{for every }1\leq j\leq\ell .
\tag{1}
$$

Let $Q_{\mathrm{WTPath}}(n)$ denote the minimum number of oracle queries used by a quantum algorithm that outputs a valid path satisfying (1) with bounded error on the standard random Welded Tree oracle distribution. The open question is whether there exists a constant $c>0$ such that, for all sufficiently large $n$,

$$
Q_{\mathrm{WTPath}}(n)
 \geq
 2^{cn}.
\tag{2}
$$

Equivalently, does the exponential quantum-query lower bound in (2) hold for unrestricted quantum algorithms?
