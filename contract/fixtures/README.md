# Fixtures

`ledger/` is a sample main ledger; `activity/` is the matching sample of the second repository, with a small `artifact-store/` standing in for the object store.

Four problems were migrated from the legacy catalog slice under `catalog/` and carry real content: the solved qubit bi-negativity problem, the partially solved Ruskai multiplicativity problem, the open Kashaev volume conjecture, and the candidate Pauli-channel capacity problem from `open_problem_v2`. Their audit reviews are attributed to a placeholder human actor.

`example-conformance-problem` and everything under it is synthetic. It exercises a human proposal, admission by two independent AI reviews, a research trajectory that formulates and refutes an auxiliary problem, an attempt report with claims and artifacts, independent AI verification, automatic acceptance, a status change, and a comment. The mathematics in it is placeholder text.

Records are immutable and the ids are fixed; do not regenerate by hand.
