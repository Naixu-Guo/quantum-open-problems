{
  "name": "quantum-open-problems-web",
  "private": true,
  "type": "module",
  "description": "The web app: static files the service serves, a client of its API.",
  "scripts": {
    "test": "node --experimental-strip-types --no-warnings --test test/*.test.mjs"
  },
  "engines": { "node": ">=22.13" }
}
