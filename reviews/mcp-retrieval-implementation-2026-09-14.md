**MCP/API 完整整合与研究读取验收 · 2026-09-14**

实现分支 `feat/mcp-research-retrieval` 从最新主分支 `e4d58d143c6d3999ae27cc35a9602f53da3be01d` 建立，承接此前尚未合并的 MCP 1.3 第一、二阶段，再完成本轮检索、研究读取与真实提问验收。科学 JSON、TeX、ledger/activity、永久标识及二元状态均保持原样；最新主分支的许可、页脚和 About 内容保留。原工作区的无关修改没有被覆盖或混入。

本轮落地内容：

- **科学搜索。** 完整 ID/alias 单独匹配，排除 ULID 中 `SVP` 的偶然子串。受控支持 QMA(2)/QMA2/QMA 2、stabilizer/stabiliser、SVP/完整名称和常见中文领域词。保留符号和 SQL 通配字符的字面语义，不对数学符号做任意词干处理。按标题、命题、进展等字段排序，返回命中字段、相对分数及原文片段；明确限制为 2048 字符、32 个规范化词，超限报错。
- **研究读取。** 默认详情增加 `qop-research/1`，完整分出 Source、Progress、Comment、References；逐项保留 TeX、引用键和修订定位。`view=research` 仅在正文与导出清单中的目录预期投影相符时省略重复 body；后续服务新增正文完整保留并标记 `bodyDisposition`，同时保留完整正式命题、条件、来源、服务评论及决议。默认 full 向后兼容；原生服务题无目录快照时保留其 body 并明确说明，不能推断没有研究历史。
- **随机抽题。** 新增 `sample_problem`，服务端从全部匹配候选均匀抽样，默认 Unsolved，返回候选数量、目录版本与完整研究视图。无匹配返回 null，不接受分页，不缓存随机结果。
- **分页变化检测。** 保留 offset，同时增加签名 cursor，绑定查询、排序、目录版本，一小时有效。目录变化明确返回 `catalog_changed`/409，参数不符、篡改或过期分别报错。这是检测变化后重查，不是保存历史快照。抽题本身不需要分页。
- **目录日期。** 普通列表按 TeX git author 编辑时间、创建时间、原始 op_ ID 排序，与网站一致。跨纯重命名跟踪，缓存正确处理 revert；活动提交复用历史结果。缺少网站目录、浅克隆或目录原稿与导入快照不符时日期未知。字段明确只是编辑历史，不冒充解决、提交或发表日期。
- **统一部署契约。** API 继续提供 `qop-context/2` 和 `qop-idempotency/2`，新增 `retrievalVersion=qop-retrieval/1`。部署检查同时确认三项；新读取面对旧 API 返回明确的不兼容错误。官方 SDK 对新搜索、研究视图和抽样结果做输出校验。

第一、二阶段已有的完整正式上下文、来源摘要、持久化幂等、双传输和取消保护一并保留，细节见 [此前实施记录](mcp-phase12-implementation-2026-09-14.md)。本次 API 升级不要求更换已固定的 SDK 依赖。

**真实提问的模型验收**

使用 `gpt-6-astra`、ultra，由模型自行选择工具和参数；工具链为真实 MCP HTTP → 真实 API HTTP → 最新 111 题维护目录。MCP 只读，服务存储在内存中。未在提示中指定工具序列、目标题目或期待答案。

| 用户原话 | 实际行为与核对结论 | 单次调用/耗时 |
| --- | --- | --- |
| 来给我抽个量子算法的题 | 调用 taxonomy 和 sample，从 25 道未解决算法题中随机抽到低能量哈密顿量模拟精度问题。答案保留参数区间、访问模型及下界适用范围 | 2 次；67.03 秒 |
| 现在这里面哪个你觉得最容易解决 | 读取比较 25 道未评级算法题，主观推荐虚拟信道共轭的两份量子比特态特例；明确 5/3 是上界，完整问题尚未解决 | 28 次；99.00 秒 |
| 最近有哪个题刚被解决 | 读取 12 道 Solved 题后介绍普通 Petz CMI 反例，准确区分 9 月 10 日提交、9 月 12 日目录核验和论文发表；没有声称外部同行评审或历史首创 | 15 次；76.25 秒 |

三份答案均经过独立的目录证据核对。日志中实际读取的 1 / 25 / 12 份问题结果，其 status、正式 main clause、Source、Progress、Comment 与权威 JSON 原稿逐字一致。答案分别保存在 [抽题](mcp-retrieval-eval-2026-09-14/case-1.md)、[切入点建议](mcp-retrieval-eval-2026-09-14/case-2.md)、[最近解决](mcp-retrieval-eval-2026-09-14/case-3.md)，调用参数、用量及环境见 [结果记录](mcp-retrieval-eval-2026-09-14/results.json)。

三个模型案例运行于最终服务正文保留修复之前，未在该修复后重跑。它们验证原目录的检索、研究历史和回答证据；最终正文保留行为由新增回归及针对性审查验证，不能将原模型结果视为对该行为的模型验收。

评价边界：只有三个单次运行，不能据此报告稳定成功率、p95 或性能优势。第二个问题为了完整比较读取了整个算法候选集，28 次调用仍有成本；本轮优先保证证据与范围。token 数为 CLI 的聚合统计，包含宿主和模型开销，不是单独的 MCP 内容用量。科学正确性按维护目录核对，没有重新证明全部外部文献。

第二、三句在独立临时会话中携带前面的用户/助手文本，没有重放工具状态。首次运行 stderr 记录过一次失败的内部协作尝试；全部成功取证来自 QOP MCP，无 shell/web/其他来源。后续 harness 已显式禁用 multi-agent。原始工具轨迹保留于本机 `/private/tmp/qop-mcp-model-eval-20260914`，可核对所附摘要哈希；轨迹排除了 reasoning 事件。

模型测试最初被自动审批以“可能发送私有目录内容”为由拒绝。核对 1,497 个输入文件与公开 GitHub 提交逐字一致、无额外文件，并确认仓库公开后，原测试命令通过复审执行；未绕过该拒绝。

**实现验证与交付边界**

真实 SDK 业务测试覆盖误匹配、完整身份、研究历史、日期证据、无评级、全候选抽样边界、游标遍历、参数校验与随机响应不缓存。另用实际 git commit → ledger 导出 → 原 MCP 连接续页验证目录变化返回 409，并可重查恢复。搜索和目录日期分别有 14 项及 6 项专门回归。

全量验证：根目录 96/96、Contract 30/30、Service 114/114、MCP 64/64、Web 11/11，共 315 项通过；Contract/Service/MCP 类型检查与 111 题站点构建通过。`check-ledger` 报告 0 个变化文件。额外逐条对照构建后的网站索引，API 的 111 题顺序、秒级编辑时间和创建时间完全一致。

首轮 Codex ultra 对抗审查发现 1 项 P2：导入题目经过服务正文修订后，research 视图仍把新增正文当重复内容省略。跟进还发现，导出器三方合并保留服务正文后也会固定新修订，因此仅比较已固定修订仍不充分。最终改为比较清单中目录预期正文和 authoredCatalog 的字段哈希（不是合并后的文件正文）；没有可信导入依据、原生背景及后续正文改动全部保留，返回 bodyDisposition，并明确提示模型读取。新增真实目录修订回归覆盖新增正文、未改正文和未验证快照，相关 14 项读取/context 测试通过。新增端到端回归实际提交合法服务正文修订，再仅改目录标题并导出、提交；原 MCP 连接读取两种视图均保留新增内容。一次跟进 CLI 审查在取证途中被内容风险门禁中断，没有产生完成报告；最终以 `gpt-6-astra`、ultra 完成内容保留专项复核，未发现剩余可复现缺陷；审查方执行 6 项读取回归与无漂移导出检查，真实 HTTP 连续更新测试由主执行方另行跑通。见[最终复核记录](mcp-retrieval-adversarial-signoff-2026-09-14.md)。本报告不表示功能已经合并或部署。发布应先启动匹配 API，再从候选 MCP 版本运行 `check:service`，最后启用 MCP；网站指南应随这一整包发布。
